
import { riskType } from 'hudson/helpers/risk-type';
import { module, test } from 'qunit';

module('Unit | Helper | risk type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = riskType([42]);
  assert.ok(result);
});

